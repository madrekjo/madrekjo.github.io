-- ============================================================================
-- ★ إضافة رتبة المالك (owner) للـ enum — شغّله أولاً بمفرده ★
-- Supabase → SQL Editor → New Query → Paste → Run
--
-- ملاحظة: يجب تشغيل هذا الملف أولاً لوحده (يُضيف القيمة للـ enum)،
-- ثم ملف 20260915000003_add_owner_role_rest.sql بعده.
-- (إضافة قيمة enum جديدة واستخدامها في نفس المعاملة يمنعه PostgreSQL)
-- ============================================================================

ALTER TYPE public.app_role ADD VALUE IF NOT EXISTS 'owner';

DO $$ BEGIN RAISE NOTICE '✅ تم إضافة رتبة المالك (owner) إلى app_role'; END $$;