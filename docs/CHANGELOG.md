# سجل التعديلات — مدارك جو

هذا الملف يجمع **كل تعديل مع تاريخه** — التعديلات السابقة والحالية.

آخر تحديث لهذا الملف: **2026-08-15**

---

## 🏷️ الإصدارات (Semantic Versioning)

| الإصدار | التاريخ | الوصف |
| --- | --- | --- |
| **v1.2.0** | 2026-08-08 | دمج الدردشة الدراسية داخل المنصة `/chat` — مجتمع طلابي بحسابات وقاعدة بيانات Supabase |
| **v1.1.1** | 2026-08-05 | إصلاح حركة نبض زر مُدرك — الزر يبقى ثابتاً ومقفلاً حتى الضغط |
| **v1.1.0** | 2026-08-05 | إطلاق المساعد الذكي مُدرك (قاعدة معرفة + ذكاء Llama 3.3 70B) |
| **v1.0.0** | 2026-07-31 | النسخة الأولى للمنصة |

---

## 📅 السجل الزمني

### 2026-08-15 (قسم المراجعة الختامية في حقول جيل 2009)

- **إضافة قسم "🔄 المراجعه الختاميه"** في الشريط الجانبي لكل حقول جيل 2009 (أعمال/هندسة/صحة/لغات) بنفس نمط جيل 2010 الموحد.
- **رابط المراجعة**: `../exam.html?review=true&generation=2009` — يعرض الأسئلة الخطأ لنفس الجيل بنظام exam.html الموحد.
- **إشعار تلقائي**: عند وجود أسئلة خاطئة محفوظة (localStorage `madrekjo_wrong_questions`) تظهر رسالة توجّه الطالب إلى المراجعة الختامية — نفس سلوك صفحة 2010.
- **النمط الموحد**: نمط الأسئلة ونظامها (روابط المواد + المراجعة) مطابق بين جميع الأقسام والجيلين.

### 2026-08-15 (تعطيل المساعد الذكي مؤقتاً)

- **إيقاف "مُدرك" (الذكاء الاصطناعي) مؤقتاً** بسبب ثغرات — يُبقي السكربت محمّلاً في الصفحات لكن دون أي تأثير (زر/لوحة/طلب API).
- **إعادة التفعيل لاحقاً**: تغيير `AI_ENABLED` إلى `true` في `js/chatbot.js` أو حذف سطري الحاجز.
- **نسخة الكاش**: رفع `sw.js` من `madrekjo-v7` إلى `madrekjo-v8` ليصل التعطيل للمستخدمين فوراً.

### 2026-08-08 (دمج الدردشة الدراسية داخل المنصة — v1.2.0)

- **دمج تطبيق الدردشة الكامل** داخل مستودع الموقع تحت `chat/` (الكود المصدري React/Vite + ملفات التشغيل النهائية الجاهزة لـ GitHub Pages).
- **روابط التشغيل**: نسخة البناء في جذر `chat/` (index.html + assets/) ليعمل الموقع مباشرة بدون أي خطوة بناء من طرف الاستضافة.
- **حاجز SPA**: إنشاء `404.html` في جذر المستودع — يخدم المسارات الداخلية مثل `/chat/auth/callback` و`/chat/profile`، ويعيد توجيه غير `/chat/*` إلى الرئيسية.
- **Service Worker**: إضافة network-first لتنقلات `/chat/` حتى لا يقدّم نسخاً قديمة بعد كل تحديث.
- **ربط أزرار "الدردشة الدراسية"** في صفحات 2009 (هندسي/صحي/أعمال/لغات) و2010 إلى `/chat/`.
- **رابط الفوتر** في `index.html` أصبح يفتح الدردشة بدل ديسكورد.
- **حل تعارض البناء**: إدخال `entry.html` كمدخل Vite + سكربت `build-fix.mjs` ينقل مخرجات البناء تلقائياً إلى جذر `chat/`.
- **الهدف من النقل**: حساب واحد وتجربة واحدة على نفس النطاق، وصيانة أسهل بمستودع واحد، وربط أصدق للطلاب.
- **الهدف من قاعدة البيانات (Supabase)**: حفظ الحسابات والرسائل والملفات والأدوار بأمان — أساس نمو قاعدة المستخدمين وتراكم مجتمع الطلاب.
- **التحقق**: 13 اختبار متصفح حقيقي + اختبارات HTTP على محاكاة GitHub Pages — كلها ناجحة بدون أخطاء Console.
- **الرفع**: commit `1025ad3` → `origin/main` → النشر التلقائي على GitHub Pages.

### 2026-08-08 (اختبار ما بعد الرفع)
- `https://madrekjo.github.io/chat/` يعمل ويعرض التطبيق مباشرة.
- `/chat/auth/callback` يُرجع 404 في بروتوكول HTTP (سلوك GitHub Pages الطبيعي) لكن `404.html` تشغّل التطبيق في المتصفح ليعالج تسجيل الدخول الراجع.

### 2026-08-05 (جلسة المساعد الذكي مُدرك — v1.1.0)

- **15:20** — إطلاق **مُدرك** 🎉: مساعد ذكي كامل مدمج بكل صفحات الموقع.
  - قاعدة معرفة كبيرة (90+ إدخال) عبر 3 أقسام: عن المنصة / الدراسة والامتحانات / المساعدات والدعم.
  - ذكاء اصطناعي **Llama 3.3 70B** عبر Cloudflare Workers AI.
  - شارة "🎯 فهمت من سؤالك" + أزرار أقسام + أزرار تنقل ذهبية (نفس التبويب).
  - **فهم موقع المستخدم** من الصفحة مباشرة (المسار/الجيل) دون تخمين من كلامه.
  - **دعم الإنجليزية** (ردود وكلمات وجواب احتياطي) + **ذاكرة محادثة** (6 تبادلات).
  - إصلاح قاعدة شرح الدروس: مُدرك يشرح المواد من معرفته العامة.
  - المعلومات: بداية المنصة في أكتوبر 2025، وفوائدها.
  - أجوبة التسجيل تُحدَّث: أغلب المنصة بدون حسابات، وبعض الأقسام فقط تتطلب تسجيل دخول.
- **15:48** — إصلاح **حركة نبض الزر** (v1.1.1): إزالة `mkPulse` من `css/chatbot.css` — الزر العائم ثابت.
- **16:0x** — إضافة الوثائق الاحترافية:
  - `README.md` — سجل/ملف الشركة (المهمة، الرؤية، القيم، الأرقام، خارطة الطريق، الإصدارات).
  - `PRIVACY.md` — سياسة الخصوصية (دقيقة لواقع المنصة: بدون حسابات، تخزين محلي، معالجة AI).

### 2026-07-08
- **إنشاء `coming-soon.html`** — صفحة "قريباً" للأقسام قيد التطوير.

### 2026-07-13
- **إضافة `serve.js`** — خادم محلي للتطوير (منفذ 8000).

### 2026-07-31 (صباحاً — النسخة الأولى والرفع)
- **07:10** — تحديث `exam.html` (محرك الاختبارات والمراجعة الختامية).
- **08:23** — إضافة `.gitignore`.
- **08:24** — الـ commit الأولي:
  `النسخة الأولى لمنصة مدارك جو - أقسام 2009 و2010 والاختبارات ونظام الإنجاز`
  (شمل وقتها: نظام التسجيل، تطبيق الإنجاز، لوحة التحكم، والمكونات المرتبطة).
- **08:43** — إزالة نظام التسجيل من `2009/index.html`:
  حذف سكربت `supabase.js`، شارة الملف الشخصي، نافذة الملف الشخصي، و`config.js`.
- **08:54** — تعديل صفحات الحقول الأربعة (`languages/engineering/health/business`) و`2010/index.html` و`js/main.js`.
- **08:58** — تنظيف `css/style.css`.
- **08:59** — تحديث `index.html` وإنشاء مجلد `docs/`.

### 2026-07-31 (جلسة أيقونات التواصل)
- **استخدام صور حقيقية لأيقونتي إنستغرام وديسكورد** بدل الأيقونات المرسومة بـ CSS:
  - إضافة مجلد `assets/icons/` مع `instagram.png` و`discord.png`.
  - استبدال أيقونة `ig-glyph` وشعار SVG بـ `<img>` في جميع الصفحات السبعة (مع مسار صحيح لكل مستوى).
  - تحديث `css/style.css` والـ Media Queries لتنسيق الصور الجديدة.
- **تحديد حجم الصور بالبكسل** (30px و28px) مع `max-width/max-height: 100%` بحيث تبقى محصورة داخل حجم الأيقونة على كل الشاشات.
- **قصّ الصور على الشعار نفسه وإعادة تحجيمها إلى 96×96** (كانت 767×767 و1280×720 مع فراغ شفاف كبير يجعلها تظهر ضخمة)، وإضافة `width/height` مباشرة في وسم `<img>` لضمان الحجم حتى بدون CSS.

### 2026-07-31 (جلسة إصلاح أزرار التواصل)
- **إصلاح شكل زرّي إنستغرام وديسكورد العائمين** في `css/style.css`:
  - عدسة إنستغرام: إزالة الإطار الأبيض الذي كان يحيط بالتدرج، وتوسيطها بشكل دقيق، واستخدام تدرج خطي ناعم بدل التدرج المخروطي (كانت تظهر مزدحمة ومشوهة).
  - نقطة الكاميرا: تصغيرها من 15% إلى 11% ومحاذاتها بشكل صحيح.
  - زر ديسكورد: إزالة `filter: drop-shadow` الذي كان يجعل الشعار ضبابياً.

### 2026-07-31 (جلسة التنظيف الشامل — الأقسام المحذوفة)
- **حذف نهائي للمكونات المرتبطة بنظام التسجيل:**
  - `admin/index.html` — لوحة تحكم المشرف (كانت تعتمد على تسجيل الدخول).
  - `achievement/` — تطبيق الإنجاز (React + حزم Supabase).
  - `al-injaz-source/` — مصدر تطبيق الإنجاز (بما فيه `supabase/migrations`).
  - `sql/` — مخطط قاعدة البيانات وإيميلات الأدمن.
  - `js/config.js` و`js/lib/supabase.js` — مفاتيح الاتصال.

---

## 📂 سجل الملفات (آخر تعديل لكل ملف)

| الملف | الغرض | آخر تعديل |
| --- | --- | --- |
| `coming-soon.html` | صفحة "قريباً" | 2026-07-08 13:56 |
| `serve.js` | الخادم المحلي | 2026-07-13 13:14 |
| `exam.html` | محرك الاختبارات والمراجعة الختامية | 2026-07-31 07:10 |
| `.gitignore` | استثناءات git | 2026-07-31 08:23 |
| `2009/index.html` | اختيار التخصص — جيل 2009 | 2026-07-31 08:43 |
| `2009/languages.html` | العلوم الإنسانية والاجتماعية | 2026-08-15 |
| `2009/engineering.html` | الهندسة وتكنولوجيا المعلومات | 2026-08-15 |
| `2009/health.html` | الطب والعلوم الصحية | 2026-08-15 |
| `2009/business.html` | الاقتصاد وإدارة الأعمال | 2026-08-15 |
| `2010/index.html` | المواد المشتركة — جيل 2010 | 2026-07-31 08:54 |
| `js/main.js` | المنطق المشترك | 2026-07-31 08:54 |
| `css/style.css` | التنسيقات | 2026-07-31 (إصلاح أزرار التواصل) |
| `index.html` | الصفحة الرئيسية | 2026-07-31 08:59 |
| `README.md` | الملف التعريفي للشركة (جديد) | 2026-08-05 |
| `PRIVACY.md` | سياسة الخصوصية (جديد) | 2026-08-05 |
| `js/chatbot.js` | مُدرك: قاعدة المعرفة + المحرك + الواجهة (جديد) | 2026-08-15 |
| `css/chatbot.css` | أنماط مُدرك (جديد) | 2026-08-05 |
| `cloudflare-worker.js` | موصل مُدرك بـ Workers AI (جديد) | 2026-08-05 |
| `tools/test-chatbot.js` | اختبارات مُدرك الآلية — 56 اختباراً (جديد) | 2026-08-05 |
| `tools/test-chatbot-load.js` | اختبار تحميل المُحرك (جديد) | 2026-08-05 |
| `docs/PROJECT.md` | توثيق المشروع | 2026-07-31 08:59 |
| `docs/CHANGELOG.md` | سجل التعديلات (هذا الملف) | 2026-08-08 |
| `404.html` | حاجز SPA للمسارات الداخلية للدردشة (جديد) | 2026-08-08 |
| `chat/` | تطبيق الدردشة الدراسية كاملاً — مصدر + بناء (جديد) | 2026-08-08 |
| `2009/engineering.html` وغيرها | زر "الدردشة الدراسية" → `/chat/` | 2026-08-08 |
| `sw.js` | network-first لتنقلات `/chat/` | 2026-08-15 |

---

## ✅ ملخص تعديلات جلسة 2026-08-08 (v1.2.0)

- **عودة الدردشة الدراسية** كجزء من المنصة على `/chat` — بنفس النطاق.
- **دمج الكود** في `chat/` (مصدر React + ملفات تشغيل جاهزة لنشر GitHub Pages مباشرة).
- **قاعدة بيانات Supabase** للحسابات والرسائل والملفات والأدوار — أساس نمو قاعدة المستخدمين.
- **حاجز SPA** عبر `404.html` ليعمل تسجيل الدخول الراجع (`/chat/auth/callback`) والمسارات الداخلية.
- **تحديث Service Worker** لدعم التحديثات بدون نسخ قديمة.
- **ربط أزرار الدردشة** في 5 صفحات (2009×4 + 2010) + رابط الفوتر الرئيسي.
- **اختبارات**: HTTP على محاكاة GitHub Pages + متصفح حقيقي (13 اختباراً) — كلها ناجحة.

---

## ✅ ملخص تعديلات جلسة 2026-07-31

- **استبدال أيقونة إنستغرام** بأيقونة مخصصة (CSS) في 7 ملفات HTML.
- **إزالة نظام التسجيل بالكامل** (Supabase) من كل الصفحات.
- **حذف** admin / achievement / al-injaz-source / sql / config.js / supabase.js.
- **أقسام قيد العمل**: الدردشة الدراسية، الإنجاز، وأنا مجهول → تشير إلى `coming-soon.html`.
- **إزالة كود ميت**: كتلة DRAGGABLE PROFILE BADGE من `js/main.js` + 528 سطراً من CSS الميت.
- **تحديث الروابط** من farmbot1238.github.io إلى madrekjo.github.io.
- **إنشاء التوثيق**: `docs/PROJECT.md` و`docs/CHANGELOG.md`.
- **إصلاح شكل زرّي إنستغرام وديسكورد العائمين** (CSS).
- **استخدام صور حقيقية** لأيقونتي إنستغرام وديسكورد بدل الأيقونات المرسومة.

---

## ✅ ملخص تعديلات جلسة 2026-08-05 (v1.1.0 + v1.1.1)

- **إطلاق المساعد الذكي مُدرك** على كل صفحات الموقع (`js/chatbot.js` + `css/chatbot.css`).
- **ذكاء اصطناعي Llama 3.3 70B** عبر Cloudflare (`cloudflare-worker.js` + `AI.endpoint`).
- **قاعدة معرفة كبيرة** (90+ إدخال) بثلاثة أقسام داخل المحادثة + دليل مساعدة للنموذج.
- **فهم موقع المستخدم**: يُحدَّد من الصفحة مباشرة (المسار/الجيل) — لا تخمين من كلام المستخدم.
- **دعم الإنجليزية** + **ذاكرة محادثة** (6 تبادلات) + **درجة إبداع** temperature 0.7.
- **إصلاح قاعدة شرح الدروس**: مُدرك يشرح المواد الدراسية من معرفته العامة.
- **إزالة حركة النبض** من زر مُدرك العائم (v1.1.1).
- **تحديث أجوبة التسجيل**: أغلب المنصة بدون حسابات، وبعض الأقسام فقط تتطلب تسجيل دخول.
- **توثيق**: `README.md` (ملف الشركة) + `PRIVACY.md` (سياسة الخصوصية) + تحديث `docs/CHANGELOG.md`.
- **اختبارات آلية**: 56 اختباراً (`tools/test-chatbot.js`) + اختبار تحميل — كلهم ناجحين.
- **الرفع**: 3 commits إلى `origin/main` → النشر التلقائي على GitHub Pages.
