# تقرير الفحص الكامل لمنصة مدارك جو

تاريخ الفحص: 2026-09-18
النطاق: كل الموقع (كل الأقسام + قاعدة Supabase)
العمق: أمني + منطقي

---

## 1) ملخص تنفيذي

| المقياس | النتيجة |
|---|---|
| أقسام تبنى بنجاح | **9/9** (كل التطبيقات ذات package.json) |
| فحص أنواع TypeScript نظيف | **9/9** |
| أسرار service_role مكشوفة | **لا يوجد** (فحص آلي لكل الملفات) |
| أخطاء البناء/إيقاف الصفحة | **0** |
| مشاكل حرجة مؤكدة | **4** |
| مشاكل عالية | **5** |
| مشاكل متوسطة | **8** |
| مشاكل منخفضة | **~11** |

**أهم سطر لك كمدير:** الموقع لا «ينكسر من تراكم أخطاء». كل قسم يُبنى كنسخة واحدة كاملة، وأي خطأ لغوي يوقف البناء بنفسه قبل النشر. لكن الفحص كشف **4 مشاكل حرجة حقيقية** تحتاج معالجة، أبرزها أنها متعلقة بالخصوصية وصلاحيات القاعدة — وكلها قابلة للإصلاح ولا تتطلب إعادة بناء الموقع.

---

## 2) ماذا شغّلنا فعلياً (المنهجية — قابلة لإعادة التشغيل)

1. **بناء ونشر**: `vite build` لكل تطبيق ذي package.json (9 تطبيقات) — كلها تنجح.
2. **فحص الأنواع**: `tsc --noEmit` لكل تطبيق — كلها نظيفة.
3. **اختبارات وحدة**: `vitest` (chat: 7 ✓ بعد أن أضفت 6 اختبارات جديدة لمنطق المنشن)، `node --test` (question-factory: 23 ✓)، اجر وثواب: 1 ✓.
4. **فحص أسرار آلي**: مسح كل الملفات عن `service_role`/مفاتيح سرية — نظيف، وملفات `.env` الحساسة غير متعقّبة في git (مؤكد بـ gitignore).
5. **مسح SQL آلي**: 162 ملف SQL ✓ — لا توجد سياسة تُنشأ مرتين في ملف واحد داخل مجلدات `migrations` الفعلية (التكرار موجود فقط في ملفات تصدير/نسخ احتياطي قديمة غير مطبقة).
6. **مراجعة أمنية/منطقية عميقة**: 3 مراجعين مستقلين (sub-agents) راجعوا كل قسم بدون ثقة مسبقة بعملي، **ثم تحققتُ بنفسي بنظري من كل ادعاء حرج** قبل إدراجه هنا.

---

## 3) حالة الأقسام

| القسم | بناء | أنوع | اختبارات | أمن/منطق |
|---|---|---|---|---|
| chat (دردشة جو) | ✅ | ✅ | ✅ 7/7 | ⚠️ 3 حرجة |
| بين السطور (sutur) | ✅ | ✅ | — | ⚠️ 2 حرجة |
| قسم اجر وثواب | ✅ | ✅ | ✅ 1/1 | ❌ النشر معطوب |
| قسم اكتب سؤالك | ✅ | ✅ | — | ⚠️ تسريب الإجابات |
| قسم انا مجهول | ✅ | — | — | ⚠️ منخفضة (lint: 1206 ملاحظات تنسيق فقط) |
| قسم ملفات المعلمين | ✅ | ✅ | — | ⚠️ منخفضة (SVG مسموح) |
| achievement | ✅ | ✅ | — | ⚠️ متوسطة (مزامنة كلمة المرور) |
| ops + ops-worker | ✅ | ✅ | — | ⚠️ متوسطة (كلمة مرور بالحزمة) |
| question-factory | ✅ | — | ✅ 23/23 | ✅ سليم |
| صف حاوي: chat-worker / sso-worker / anon-ip-worker | — | — | — | ⚠️ متوسطات |
| جذر الموقع + static | ✅ | — | — | ✅ سليم |

---

## 4) المشاكل الحَرِجَة (أولوية الإصلاح)

### K1 — بين السطور: «ركن القرّاء» يسرّب هوية أي قارئ (device_id) → التحكم الكامل بحسابه
- **المصدر**: `install_all.sql:357-378` (دالة `get_chat_messages` تُعيد العمود `device_id` كاملاً) + سياسة `reader_chat_select_anon (using true)` (`install_all.sql:1261-1263`) + Realtime يبث الرسالة (ومعها `device_id`) لكل المشتركين.
- **الأثر**: `device_id` هو كلمة السر الوحيدة في القسم (كل الدوال `security definer` تستقبله كمعرّف). أي زائر يقرأ شات بين السطور يلتقط `device_id` أي مستخدم ثم يستخدمه لتنفيذ `set_username/set_avatar/set_bio/delete_line/rate_user/toggle_follow` باسمه.
- **الحل المبدئي**: إخفاء عمود `device_id` من `get_chat_messages` (إرجاع صف نظيف بلا `device_id`)، وضرورة إعادة أرشفة سياسية قراءة Realtime.

### K2 — chat: دوال النقاط تعمل على أي user_id بدون تحقق هوية
- **المصدر**: `chat/supabase/migrations/20260907000001_points_50_100_reward10.sql` — `spend_points(p_user_id,…)` (سطر 28) و`reward_round_time` و`get_user_points` لا تفحص `auth.uid()` إطلاقاً (تحققت بنفسي).
- **الأثر**: أي مستخدم مسجّل يستطيع الاتصال المباشر بـ RPC كي **يصفّر رصيد أي مستخدم آخر** أو يمنح نقطاً لنفسه ولحسابات بديلة (رفع سقف الجوائز دورياً).
- **الحل**: داخل كل دالة أضف شرط `auth.uid() = p_user_id` (أو أعد هيكلة لقراءة صاحب الطلب تلقائياً).

### K3 — chat: بريد كل المستخدمين مقروء لأي عضو مسجّل
- **المصدر**: عمود `email` في `profiles` (تُملأ من auth.users في `20260903000000`) + سياسة القراءة `"Authenticated users can view profiles" USING(true)` (`20260604041914`).
- **الأثر**: أي طالب يقرأ REST مباشرة يسلّب كل بريد المسجّلين (والواجهة تُخفيه عن غير الأدمن فقط — الحماية شكلية).
- **الحل**: حذف `email` من سياسة القراءة العامة أو إزالته من `profiles` (يُقرأ من `auth.users` عند الحاجة عبر دالة `security definer` محصورة بالأدمن).

### K4 — قسم «أجر وثواب» في الموقع الرئيسي معطوب (صفحة فاضية)
- **المصدر**: `قسم اجر وثواب/dist/index.html:13-14` = `ajr/index.html` يستشهد بأصول `/assets/*.js` (جذرة) بينما `ajr/assets/` لا يملكها — سببه غياب `base` في `vite.config.ts`.
- **الأثر**: بطاقة «أجر وثواب» في الرئيسية تفتح صفحة لا تعمل (بينما `ajr.html` الثابتة تعمل).
- **الحل**: إضافة `base: "/ajr/"` وإعادة البناء/النسخ.

---

## 5) مشاكل عالية

- **[chat] ضمان النقاط «واجهة فقط»**: سياسات INSERT على posts/comments لا تفرض رصيداً؛ الخصم استدعاء اختياري بعد النشر (`Chat.tsx:464-476`). مستخدم برصيد 0 ينشر بلا تكلفة عبر REST مباشر. الحل: فرض الفحص داخل SQL (دالة نشر واحدة تجمع الإدراج+الخصم).
- **[بين السطور] صلاحيات التخزين بلا ملكية**: سياسات `avatars_public_update/delete` و`proofs_*` تسمح لأي شخص (بيئة anon/authenticated) بالكتابة فوق/حذف أي ملف بقيد `bucket_id` فقط — وخطرها يتضاعف مع K1 (المسارات تبدأ بـ device_id المكشوف). الحل: ربط سياسات الـ delete/update بمسار يخص الجهاز عبر RLS شرطي على `storage.foldername`.
- **[بين السطور] تضخيم المشاركات بلا دليل**: `record_share`/`confirm_share` يزيدان عداد `shares` لأي بطاقة دون التحقق من ملكية السطر أو وجود سكرين شوت فعلي — نظام «المتحدث يحسب» قابل للتلاعب.
- **[اكتب سؤالك] الإجابة الصحيحة تُرسَل للعميل**: `قسم اكتب سؤالك/src/lib/db.ts:52-56` يضم `correct` في اختيار القراءة، والفحص محلي (`App.tsx`). أي طالب يقرأها من DevTools. الحل: حذف `correct` من الاستعلام العام والتحقق خادمياً عند الإجابة.
- **[بين السطور] التقييم الذاتي + صناعة أجهزة وهمية**: `rate_user` لا يمنع قيّم المنصة نفسه من تقييم حسابه، ولا حدود مفيدة للأجهزة المزيفة → تضخيم تقييم النجوم.

---

## 6) مشاكل متوسطة

1. **[chat]** التفاف على مَنع منشن الجنس الآخر عبر تعديل `gender` ذاتياً (سياسة تحديث `profiles` بلا قيد عمود وبدون trigger يحمي `gender`).
2. **[chat]** إشعارات المجموعة لا تستثني المحظورين ولا تحصر القناة ولا تمنع التكرار/حشد مستلمين (مذا يفضي لضخ إشعارات بلا حد).
3. **[chat]** المنشن الفردي يعبر سور الجنس/القناة: شاب يذكر اسم بناتية من قناة الشباب فتصلها الإشعارات.
4. **[chat]** المحظور/الموقوف قادر على إنشاء منشن جماعي عبر API (لا trigger ban على `post_mentions`).
5. **[chat]** مفارقة رتب: المالك (owner) لا يستطيع `@الجميع` (الحارس يعترف بـ admin فقط)؛ والنقاط تستثني staff لكن ليس owner.
6. **[بين السطور]** جميع الحدود مرتبطة بـ `device_id` قابل للتغيير/التقليد (حدَ 10 سطور/ساعة و3 رسائل/دقيقة).
7. **[anon-ip-worker]** ملح هاش IP افتراضي ثابت معروف (`"madarik-anon-ip"`) + CORS يقبل أي origin → قياس هاش IP للزوار عبر مواقع خارجية.
8. **[chat-worker]** محطة `/invalidate` بلا مصادقة (يجبر الإعادة جلب ويستهلك حصة KV).
9. **[ops]** كلمة مرور مشفرة ثابتة في الحزمة العامة + تحقق محلي بلا rate-limit خادمي.
10. **[achievement]** دالة `sync-achievement-user` تعيد تعيين كلمة مرور حساب موجود وتُعيدها للمستدعي (نمط خطير حتى إن اشترطت ملكية بريد) + تناقض تعريفي بين مشروعين (`ofltana…` مقابل `itflhfhs…`).
11. **[sso-worker]** يرفض `email_verified === false` فقط، والغياب الكامل للحقل يمرّ — المطلوب اشتراط `=== true`.
12. **[bug]** `question-factory`/`achievement`: مفاتيح حية في `.env` غير متعقبة في git (آمن كملفات، لكن لا تُشارك المجلد دون تدوير المفتاح).

---

## 7) مشاكل منخفضة (لا تعرقل)

- حدود `device_id` قابلة للتلهّي بتغيير البصمة (بين السطور) — شأن تصميمي في منصة بلا مصادقة.
- `avatar_url` يقبل أي URL خارجي (قناة تتبع زوار) — بلا تنفيذ كود.
- `allowed_mime_types` لا يُعاد تطبيقه على bucket موجود عند `on conflict do update`.
- حدود ملفات المعلمين: `svg+xml` مسموح (SVG برمجي) — يُفضّل الاستبعاد.
- «انا مجهول»: رمز كسر الحظر الثابت `200920092009` معروف في كود migration.
- `support-media` (chat): رفع بلا مجلد خاص بالمستخدم.
- Cloudinary preset غير موقّع (`madarik`) → أي شخص يرفع لحساب المشروع (استنزاف).
- chat: تقسيم شارة `منشن الجميع` (owner/admin) وتكدّس عمل الدفعات غير ذري في الإشعارات.
- قسم انا مجهول: 1206 ملاحظة ESLint كلها تنسيق Prettier (لا وظيفية) — يمكن توفيرها بـ `--fix`.
- جذر الموقع: ملاحظة أن قسم تطوري (مجلد) غير متتبع في git (مساحة تطويرية).

---

## 8) مناطق سليمة مؤكدة (تحقّق المستقل + تحققي)

> جيّد جداً للاطمئنان — هذه المناطق دُقّقت عن كثب:

- ✅ **تعديلات منشن البنات/الشباب (الجديدة)**: قيد `post_mentions_all_check` الجديد يمنع استحالة دمج `is_all=true` مع `mention_group` معاً (محكوم بـ CHECK قبل أي trigger)، و`guard_group_mention` يمنع منشن الجنس الآخر، وبناء صفوف `user_id=null/is_all=false/mention_group` في الكود مطابق تماماً للحالة المسموحة — **وقد تأكد ذلك من مراجع مستقل استقلالية عن عملي**.
- ✅ **لا XSS**: نصوص المستخدم تمر عبر React escaping؛ لا `dangerouslySetInnerHTML` على محتوى المستخدم في أي تطبيق.
- ✅ **لا أسرار service_role في كود أي تطبيق**، وملفات `.env` الحساسة غير متعقّبة في git.
- ✅ **بين السطور**: RLS مفعّل على كل الجداول، وكل الدوال `security definer` بـ `search_path` مقيّد وبلا SQL ديناميكي → لا تصعيد صلاحيات.
- ✅ **تطابق api.ts مع دوال SQL** (بين السطور) — لا تعارض أسماء/معاملات.
- ✅ **هيمنة الأدمن/المالك في chat** على العمليات الحساسة مُفرَضة في SQL (تسجيل الأدوار، حذف المنشورات، Express has_role) وليست مجرد واجهة.
- ✅ **قواعد الأقسام الفرعية** (اكتب سؤالك، انا مجهول، achievement، جذر supabase) سليمة في إطارها.
- ✅ **جذر الموقع والروابط والـ sitemap** سليمة (باستثناء نقطة ajr/ في K4).

---

## 9) فجوات في التغطية الآلية

- **لن عمل ya Postgres محلي**: Docker متوقف وبدون كلمة سر لا يمكن تشغيله، لذا لم أُنفّذ الـ migrations على قاعدة حقيقية — التحليل الأمني للـ SQL تم ثابتاً. الأداة `supabase start` تحتاج Docker لتنفيذه لاحقاً إن رغبت.
- **achievement**: يملك `vitest` لكن بلا أي ملف اختبار فعلي.
- **بين السطور واكتب سؤالك وملفات المعلمين وانا مجهول**: بلا اختبارات وحدة (فجوة، لا خطأ).
- RPC административных في «أجر وثواب» و«ملفات المعلمين» غير موجودة في المستودع إطلاقاً (لا أثر للتدقيق).

---

## 10) أولويات التوصية

| الأولوية | الإجراء | القسم |
|---|---|---|
| 1 | إخفاء `device_id` من مخرجات `get_chat_messages` | بين السطور |
| 2 | فرض `auth.uid()` في دوال النقاط الثلاث | chat |
| 3 | حجب بريد المستخدمين عن القراءة الجماعية | chat |
| 4 | إصلاح `base` في اجر وثواب وإعادة النشر | اجر وثواب |
| 5 | تقييد delete/update في storage بمسار الجهاز | بين السطور |
| 6 | إخفاء `correct` عن العميل والشرح خادمياً | اكتب سؤالك |
| 7 | تفعيل الفحص الذاتي SQL وتشغيل docker لاحقاً | كل الأقسام |
| 8 | إضافة اختبارات وحدة للأقسام البلا اختبارات | متعدد |

---

# استعراض معماري تقني (Senior Review) — 2026-09-18

منهجية: مراجعة ثابتة ومرتكِزة على القياس (build/tsc/lint/jscpd/SQL + تحقق بمنزل السطر)، مقيَّدة على الكود الفعلي لا الافتراض.

## 1) Security

- **S1 — تسريب هوية الجهاز (حرجة · Critical)**
  - لماذا: دالة `get_chat_messages` تستعمل `select *` من `reader_chat` (وهي تعرف `device_id` كمفتاح الهوية الوحيد)، والبث الحيّ (Realtime) يمرّر كامل الصف للجميع. أي زائر يحصل على `device_id` أي قارئ → تحكّم كامل (تعديل/حذف باسمه).
  - الإصلاح: إسقاط `device_id` من كل مخرجات عامة، منها البث؛ عرضٌ مُنقَّح Sanitized View.
  - مثال:
    ```sql
    -- بدلاً من select *
    CREATE OR REPLACE FUNCTION public.get_chat_messages(p_limit integer DEFAULT 50)
    RETURNS TABLE(id bigint, nickname text, message text, created_at timestamptz)
    SECURITY DEFINER SET search_path = public AS $$
      SELECT id, nickname, message, created_at FROM reader_chat
      WHERE created_at > now() - interval '10 minutes'
      ORDER BY id DESC LIMIT p_limit;
    $$;
    -- وإزالة device_id من عمود البث أو استبدال البث بجلب دوري (نمط chat)
    ```

- **S2 — دوال النقاط بلا مصادقة هوية (حرجة · Critical)**
  - لماذا: دوال `security definer` (spend/rewind/invite...) تقبل `user_id` من العميل فتُصلِح رصيد أي طالب.
  - الإصلاح: اشتقاق `v_uid := auth.uid()` داخل الدالة وإسقاط البارامتر.
  - مثال:
    ```sql
    CREATE OR REPLACE FUNCTION public.spend_points(p_amount int, p_reason text)
    RETURNS void SECURITY DEFINER SET search_path = public AS $$
    DECLARE v_uid uuid := auth.uid();
    BEGIN
      IF v_uid IS NULL THEN RAISE EXCEPTION 'unauthenticated'; END IF;
      UPDATE public.points SET balance = balance - p_amount
        WHERE user_id = v_uid AND balance >= p_amount;
      IF NOT FOUND THEN RAISE EXCEPTION 'insufficient points'; END IF;
    END $$;
    ```

- **S3 — بريد المستخدمين مقروء للجميع (عالية · High)**
  - لماذا: قاعدة RLS تتيح قراءة `profiles.email` لأي authenticated. PII + إمكان استغلال للرسائل المزيفة.
  - الإصلاح: منع القراءة العامة، إخراج ملف تعريفي عام عبر دالة/View بلا البريد.

- **S4 — ملكية ملفات التخزين غير مُتحقق منها (عالية · High)**
  - لماذا: الإدراج في `post-media` مُقيَّد بمسار الـuid لكن UPDATE/DELETE لا يفحص الملكية → أي مستخدم يعدّل ملف غيره إذا خمّن المسار.
  - الإصلاح: تطبيق `storage.foldername(name)[1] = auth.uid()::text` في UPDATE/DELETE أيضاً (الأصل نفسه موجود عند INSERT).

- **S5 — إجابة «اكتب سؤالك» الصحيحة تُرجع للعميل (عالية · High)**
  - لماذا: التصحيح client-side؛ الحقل الصحيح ظاهر في استجابة الشبكة → bypass كامل.
  - الإصلاح: لا تُعدْ الإجابة أبداً؛ قارن hash وارداً server-side، أو جدول منفصل لا يُستدرج.

- **S6 — ops: كلمة سر الأدمن في الباقة (عالية · High)**
  - الإصلاح: نقل التحقق إلى دالة Worker بمتغير بيئة، لا في bundle عمومي.

- **S7 — chat-worker `/invalidate` بلا مصادقة (منخفضة-متوسطة)**
  - الإصلاح: `Authorization: Bearer <secret>` في البيئة ويكون الخادم يتحقق منه.

- **مؤكَّد سليم**: لا XSS، لا `service_role` في أي كود، لا أسرار في git، وقاعدة RLS قوية عموماً في chat (banned_devices, owner-role protections, per-uid storage insert).

## 2) Performance

- **P1 — ترقيم Offset (متوسط · Medium → عالٍ عند الكبر)**
  - لماذا: `range(offset, offset+PAGE_SIZE-1)` في Chat.tsx والإشعارات يجبر المكوّن على تخطّي N صفاً عند كل صفحة عميقة (تكلفة خطية رغم الفهارس الموجودة). اليوم الحجم صغير فالأثر طفيف.
  - الإصلاح: cursor/Keyset على `(created_at, id)`؛ والمسار الإنتاجي عبر chat-worker/feed مُخفَّف أصلاً.
  - مثال:
    ```sql
    WHERE (created_at, id) < ($2, $3)
    ORDER BY created_at DESC LIMIT $1
    ```

- **P2 — بث ركن القرّاء (متوسط)**: كل رسالة تَهمس لكل المتصلين بكامل جسمها. مرتبط بـ S1؛ الحل بجلب دوري مُنقَّح كـ chat.

- **P3 — تعدد الزيارات في صفحة الشات (منخفض-متوسط)**: الجلب مقسّم (feed + likes + comments + unread) لكن مُفعّل في صفحة واحدة؛ مقبول، ويُغلّف باحقاً في RPC واحد.

## 3) Scalability

- **SC1**: P1 هو الحدّ الأكبر للنمو.
- **SC2 — Publications بلا مشترك (منخفضة)**: `notifications/support_messages/changes_messages` في Realtime ولا مشترك فعلي → كلفة replication دائمة لكل INSERT؛ إزالة ما لا يُستعمل.
- **SC3 — تحميلٌ عمومي بلا توقيع (متوسط)**: avatars/proofs إلى Cloudinary или Storage بلا Signed upload + قواعد ملكية (منبعان لنفاد الرصيد/إساءة).
  - الإصلاح: Signed URL على الطلب، وسقف حجم، والسياسات لكل مستخدم بمسار uid.
- **SC4 (سليم)**: تقليم الإشعارات إلى آخر 10 لكل مستخدم — تصميم مقيَّد حسن.

## 4) Maintainability

- **M1 — صرامة المترجم غير موحّدة (عالية عملية · عملية)**
  - `noUnusedLocals/Parameters` معطّلة في chat وachievement واجر وثواب وانا مجهول؛ chat بلا `strict: true`؛ lint: chat 308 أخطاء أغلبها `no-explicit-any` وانا مجهول 1206 تنسيق.
  - الإصلاح: أطفئ التبسيط ثم `--fix` ثم إلزام الفحص في دورة النشر. (tsconfig: `"strict": true, "noUnusedLocals": true, "noUnusedParameters": true`).
- **M2 — `as any` رغم وجود أنواع Supabase**: إعادة توليد `supabase gen types typescript` ثم حذف الكاستات في مواضع محددة فقط.
- **M3 — نسخ قاعدة تاريخية كبيرة في مستودع chat**: تُربك مسح SQL (تعريفات سياسات مكررة)؛ انقلها لمجلد `archive/` أو من سجل git.
- **M4 — اجر وثواب/ملفات المعلمين دون migrations مقتفاة** → قواعد غير قابلة للإعادة؛ migration discipline موجود في chat وبين السطور (جيد).
- **M5 (سليم)**: التكرار المقيس منخفض جداً — chat 1.27% · بين السطور 0.82% · انا مجهول 0.51%، و0 ملف متطابق بين الأقسام الثلاثة المتشابهة.

## 5) Database inefficiencies

- **D1**: ترقيم Offset (انظر P1) — أكبر كلفة قاعدة حالياً.
- **D2 (منخفضة)**: مرشِّح الإشعارات `user_id + created_at desc` بلا فهرس صريح؛ أضِف `(user_id, created_at desc)` عند نمو الحجم — الحجم الحالي لا يبرر.
- **D3 (متوسطة)**: قائمة مستلمي «منشن المجموعة» تُجلب كاملة بلا سقف — فرض LIMIT/حدّ، واستثناء الممنوعين.
- **D4**: Publications غير المستخدمة (SC2) تضاعف الكتابة.
- **D5 (سليم)**: تغطية فهارس جيدة على posts (partial created_at), likes, comments, post_mentions, gender.

## 6) Code smells

- `any` بلا ضرورة في مواضع محدودة (Chat.tsx حول 236/246/269 وبعض ملفات الاختبار) — أوقِف عبر M2.
- الأقسام الثلاثة المتشابهة بنفس الهيكل — قد تُوحَّد في قالب مشترك **فقط** عند إضافة رابع؛ اليوم YAGNI.
- الثوابت (PAGE_SIZE, COSTS) مُمركّزة جيداً؛ أبقِ على هذا النمط.

## 7) Over-engineering

- **سلبيَّة عامة (إيجابية)**: المشروع **ليس** مبالغاً فيه. طبقة guards/triggers/policies في المنشن مبرَّرة؛ chat-worker/KV يُخفِّف الحمل فعلاً؛ لا حاجة لرفع مكتبة UI مشتركة الآن.
- الوحيد «إفراط توثيق»: نسخ القاعدة التاريخية M3 → احذفها.

## 8) Missing edge cases

- **X1 (عالية)**: نقاط تُخصم للرسالة ثم فَشْل نشرٌ لاحق (تحقق/حظر) → رصيد ضائع بلا تحصيل. الإصلاح: اعتمد `spend` بعد نجاح النشر transactionally، أو دالة صرّاف واحدة (اقْدُرْ → صرف → نشر → استرداد عند فشل).
- **X2 (عالية)**: النشر الحلو للجميع: قائمة المستلمين خارج سقف البيع أو تضم ممنوعاً → إشعارات لهدف غير مسموح. الإصلاح: ادمج فحص السقف/الحظر داخل صفقة الإدراج نفسها.
- **X3 (متوسطة)**: تقليم الإشعارات في Mobile يحذف أقدم من 10 في نفس الحساب عبر الأجهزة الأخرى. أفضل: مؤشِّر قراءة أو View بدل DELETE.
- **X4 (متوسطة)**: لا استرداد للحساب إن مُسحت بيانات المتصفح (هوية = device_id فقط). أضِف عبارة نسخ احتياطي/استرداد أو ربط اختياري بالبريد.
- **X5 (منخفضة)**: `set_username/uploadAvatar` على جهاز جديد يُنشئ حساباً صامتاً — تأكد من رسالة توضيح كي لا تتشكل صفوف يتيمة.

## الأولوية المقترحة

1. S1 + S2 + X1 (هوية ونقاط — لا إطلاق قبلها).
2. S3 + S4 (خصوصية وتملّك).
3. M1 (الصرامة) للتمدد الآمن.
4. P1/D1 + S6 (أداء وواجهة أدمن) عند النمو.

---

## 11) تذكير دائم

أي عمود/دالة/سياسة جديدة تُنفَّذ **يدوياً** في Supabase → SQL Editor من ملفات `supabase/migrations/*.sql` — ما تُنفَّذ تلقائياً عند النشر. (تفاصيل الميغريشنات الجديدة لـ«منشن البنات/الشباب» و«الاسم + الصورة» في ملفَي: `chat/supabase/migrations/20260918000000_group_gender_mentions.sql` و`قسم بين السطور/supabase/migrations/20260918_profile_avatar.sql`.)
---

## 12) حالة التنفيذ بعد الفحص (تحديث 2026-09-21)

| البند | الحالة | ملاحظات |
|---|---|---|
| S1 — سقف أحكام القرّاء | ✅ مُنفَّذ | `20260918_sanitize_reader_chat.sql` + `20260918_admin_moderation.sql` (فحص حظر داخل `post_chat_message`) |
| S3 — إخفاء بريد قرّاء chat | ✅ مُنفَّذ | `20260918000002_security_hardening.sql`: `REVOKE SELECT ON profiles` + أعمدة محدودة + `resolve_user_ids_by_email` + RPC الأدمن |
| S4 — نقاط بلا هوية من العميل | ✅ مُنفَّذ | دوال النقاط كلها `auth.uid()`؛ `publish_post` ذرّية (حص+إدراج+check) |
| S5 — عزل الإجابة الصحيحة | ✅ مُنفَّذ | `قسم اكتب سؤالك/20260918000001_hide_correct_answer.sql` + عميل عبر `submit_answer` |
| S6 — واجهة أدمن | ✅ مُنفَّذ | ركن إدارة في بين السطور بكلمة سر: حذف بطاقات/رسائل + حظر جهاز + فك حظر |
| S7 — JWT لـ /invalidate | ✅ مُنفَّذ | chat-worker يتحقق عبر JWKS قبل رفض الكاش |
| X5 — `set_username` | ✅ مُنفَّذ | داخل `20260918_profile_avatar.sql` |
| P1/D1 — ترقيم Offset في chat | ✅ مُنفَّذ (المسار المباشر) | keyset عبر `created_at` في `Chat.tsx` |
| X3 — تقليم الإشعارات | ✅ مُنفَّذ | حذف المقروء الأقدم من 3 أيام فقط بدل «آخر 10» |
| D3 — سقف مستلمي المنشن | ✅ مُنفَّذ | `.limit(200)` + فحص الحظر داخل الصفقة |
| SC4 — إبقاء آخر 10 إشعارات | ⏳ يُراجَع | استُبدل بسياسة مدة 3 أيام (أكثر أماناً لمشاركة الأجهزة) |
| M1 — صرامة المترجم | ⏳ لاحق | chat/achievement/اجر وثواب/انا مجهول |
| M3 — نقل نسخ chat التاريخية | ⏳ لاحق | إلى `chat/archive/` |
| X4 — استرداد الحساب | ⏳ مفتوح | بلا حل كامل (الهوية جهازية) — يُوثَّق للمستخدم |
| SC2 — إزالة Publications | ⏳ موقوف | حتى التحقق من اشتراكات Realtime في chat-worker |

**النشر**: بين السطور → `sutur/?v=39` · اكتب سؤالك → `ask/?v=3`.

### ميغريشنات تنتظر التنفيذ اليدوي (Supabase → SQL Editor)
1. `chat/supabase/migrations/20260918000000_group_gender_mentions.sql`
2. `chat/supabase/migrations/20260918000002_security_hardening.sql`
3. `قسم بين السطور/supabase/migrations/20260918_sanitize_reader_chat.sql`
4. `قسم بين السطور/supabase/migrations/20260918_profile_avatar.sql`
5. `قسم بين السطور/supabase/migrations/20260918_admin_moderation.sql`
6. `قسم اكتب سؤالك/supabase/migrations/20260918000001_hide_correct_answer.sql`

ثم (مرة واحدة فقط) من SQL Editor:
```sql
select public.set_admin_password('ضع-كلمة-سر-الأدمن-هنا');
```
